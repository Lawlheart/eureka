<div class="form-container" id="story-form">
  <%= form_for [blueprint, story], remote: true do |f| %>
      <%= f.label :content %>
      <%= f.text_field :content %>
      <%= f.submit %>
      <a class="button close-forms red">Close</a>
  <% end %>
</div>